enum UpPanelPosition {
  top('天灵盖'),
  leftFixed('左侧常驻，我嘞个豆'),
  rightFixed('右侧常驻，功德+1'),
  leftDrawer('左侧抽屉，功德+1'),
  rightDrawer('右侧抽屉，不是哥们'),
  ;

  final String label;
  const UpPanelPosition(this.label);
}
