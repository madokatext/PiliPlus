enum WebviewMenuItem {
  refresh('重新投胎'),
  copy('薅走这串门牌号'),
  openInBrowser('浏览器中掀开'),
  clearCache('清除电子囤货'),
  resetCookie('重新赛博调参Cookie'),
  goBack('润回去'),
  ;

  final String title;
  const WebviewMenuItem(this.title);
}
